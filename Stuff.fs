module InstableMvid.Stuff

let add (a: int) (b: int) = a + b

let minus a b = a - b

type V =
    static member X x y = x / y

// Commenting or uncommenting this class affects the mvid
// type W() =
//     class
//     end